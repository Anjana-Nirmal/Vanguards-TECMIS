
CREATE TABLE LECTURER(
    NIC      VARCHAR(20),
    Position VARCHAR(50),
    F_Name   VARCHAR(50),
    L_Name   VARCHAR(50),
    Gender   BOOLEAN,
    Email    VARCHAR(50),
    PRIMARY KEY (NIC),
    -- FOREIGN KEY (DepID) REFERENCES DEPARTMENT(DepID)

);
